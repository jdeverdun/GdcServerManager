Liste des lignes a decommenter // modifier dans la version finale : 

1. dans dicomNode metttre la bonne adresse IP de la passerelle (via interface) : private static final String DEFAULT_HOSTNAME = 120.40.30.110";
2. Dans les settings (via interface) mettre le bon nom de la DB (gdcserver et pas test)
4. Changer le User dans Mailer.java pour mettre le bon